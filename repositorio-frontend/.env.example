# Base URL del API (vacío = mismo origen; en dev Vite hace proxy de /api → backend)
# VITE_API_BASE_URL=http://localhost:3000

# Solo para vite.config: destino del proxy si el API no está en 3000
# VITE_PROXY_API=http://localhost:3000
